[package]
name = "asteroids"
version = "0.1.0"
description = "Asteroids — a faithful recreation of Atari's 1979 vector arcade original, wrapped in a modern shell."
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
macroquad.workspace = true
shell-kit = { path = "../../../shell-kit" }
asteroids-core = { path = "../core" }

[lints]
workspace = true
