[package]
name = "gnash-core"
version = "0.1.0"
description = "Pure, deterministic game core for GNASH — no engine dependency."
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true
