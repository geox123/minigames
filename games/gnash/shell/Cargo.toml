[package]
name = "gnash"
version = "0.1.0"
description = "GNASH — a faithful recreation of the 1980 arcade maze-chase original, wrapped in a modern shell."
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
macroquad.workspace = true
shell-kit = { path = "../../../shell-kit" }
gnash-core = { path = "../core" }

[lints]
workspace = true
